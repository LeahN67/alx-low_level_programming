#include "main.h"

/**
 * _puts - Entry point
 *
 * Description: prints a string to stdout
 *
 * @str: pointer to the string to be printed
 */
void _puts(char *str)
{
	int j;

	for (j = 0; str[j] != '\0'; i++)
	{
		_putchar(str[j]);
	}
	_putchar('\n');
}
